# Remove Bootstrap Infrastructure — Bare Metal (Agent-Based Installer)

## No Action Required

The Agent-based Installer does not use a dedicated bootstrap node. The rendezvous node handles the bootstrap process and transitions to a regular control plane node automatically. There is no bootstrap infrastructure to remove.
